# Pytest Fixture Failure Example

This example intentionally fails because a required fixture is missing.

Run:

```bash
errpilot run -- pytest examples/pytest_fixture_failure
errpilot bundle latest
```
