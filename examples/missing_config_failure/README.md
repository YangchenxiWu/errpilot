# Missing Config Failure Example

This example intentionally fails because a required config file is missing.

Run:

```bash
errpilot run -- pytest examples/missing_config_failure
errpilot bundle latest
```
