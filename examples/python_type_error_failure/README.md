# Python Type Error Failure Example

This example intentionally fails with a local Python TypeError inside a pytest test.

Run:

```bash
errpilot run -- pytest examples/python_type_error_failure
errpilot bundle latest
```
