# Python Import Failure Example

This example intentionally fails during pytest collection with a missing import.

Run:

```bash
errpilot run -- pytest examples/python_import_failure
errpilot bundle latest
```
